{
  "id": "pc_focus_block_detector",
  "name": "PC 深度工作块检测",
  "stage": "timeline_processor",
  "version": "0.1.0",
  "input_schema": {"type": "object", "required": ["events"]},
  "output_schema": {"type": "object", "required": ["focus_blocks"]},
  "privacy_level": "strict_local",
  "model_requirements": {"provider": "local", "modalities": ["text"], "local_only_supported": true},
  "permissions": ["event.read"],
  "prompt_template": "Detect long local PC activity blocks that look like focused work. Generated reports are hints, not final facts."
}
